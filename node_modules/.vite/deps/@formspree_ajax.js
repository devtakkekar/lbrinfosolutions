var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// node_modules/@formspree/core/dist/index.js
var require_dist = __commonJS({
  "node_modules/@formspree/core/dist/index.js"(exports, module) {
    var g = Object.defineProperty;
    var j2 = Object.getOwnPropertyDescriptor;
    var V2 = Object.getOwnPropertyNames;
    var L2 = Object.prototype.hasOwnProperty;
    var Y = (e, r) => {
      for (var t in r) g(e, t, { get: r[t], enumerable: true });
    };
    var K2 = (e, r, t, o) => {
      if (r && typeof r == "object" || typeof r == "function") for (let s of V2(r)) !L2.call(e, s) && s !== t && g(e, s, { get: () => r[s], enumerable: !(o = j2(r, s)) || o.enumerable });
      return e;
    };
    var $2 = (e) => K2(g({}, "__esModule", { value: true }), e);
    var h2 = (e, r, t) => new Promise((o, s) => {
      var i = (a) => {
        try {
          l(t.next(a));
        } catch (m) {
          s(m);
        }
      }, c = (a) => {
        try {
          l(t.throw(a));
        } catch (m) {
          s(m);
        }
      }, l = (a) => a.done ? o(a.value) : Promise.resolve(a.value).then(i, c);
      l((t = t.apply(e, r)).next());
    });
    var W = {};
    Y(W, { SubmissionError: () => p2, appendExtraData: () => E2, createClient: () => R, getDefaultClient: () => U, isSubmissionError: () => A });
    module.exports = $2(W);
    var u = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
    var J = /^(?:[A-Za-z\d+\/]{4})*?(?:[A-Za-z\d+\/]{2}(?:==)?|[A-Za-z\d+\/]{3}=?)?$/;
    function I2(e) {
      e = String(e);
      for (var r, t, o, s, i = "", c = 0, l = e.length % 3; c < e.length; ) {
        if ((t = e.charCodeAt(c++)) > 255 || (o = e.charCodeAt(c++)) > 255 || (s = e.charCodeAt(c++)) > 255) throw new TypeError("Failed to execute 'btoa' on 'Window': The string to be encoded contains characters outside of the Latin1 range.");
        r = t << 16 | o << 8 | s, i += u.charAt(r >> 18 & 63) + u.charAt(r >> 12 & 63) + u.charAt(r >> 6 & 63) + u.charAt(r & 63);
      }
      return l ? i.slice(0, l - 3) + "===".substring(l) : i;
    }
    function O2(e) {
      if (e = String(e).replace(/[\t\n\f\r ]+/g, ""), !J.test(e)) throw new TypeError("Failed to execute 'atob' on 'Window': The string to be decoded is not correctly encoded.");
      e += "==".slice(2 - (e.length & 3));
      for (var r, t = "", o, s, i = 0; i < e.length; ) r = u.indexOf(e.charAt(i++)) << 18 | u.indexOf(e.charAt(i++)) << 12 | (o = u.indexOf(e.charAt(i++))) << 6 | (s = u.indexOf(e.charAt(i++))), t += o === 64 ? String.fromCharCode(r >> 16 & 255) : s === 64 ? String.fromCharCode(r >> 16 & 255, r >> 8 & 255) : String.fromCharCode(r >> 16 & 255, r >> 8 & 255, r & 255);
      return t;
    }
    var G = () => navigator.webdriver || !!document.documentElement.getAttribute(O2("d2ViZHJpdmVy")) || !!window.callPhantom || !!window._phantom;
    var y2 = class {
      constructor() {
        this.loadedAt = Date.now(), this.webdriver = G();
      }
      data() {
        return { loadedAt: this.loadedAt, webdriver: this.webdriver };
      }
    };
    var S2 = class {
      constructor(r) {
        this.kind = "success";
        this.next = r.next;
      }
    };
    function w2(e) {
      return "next" in e && typeof e.next == "string";
    }
    var b = class {
      constructor(r, t) {
        this.paymentIntentClientSecret = r;
        this.resubmitKey = t;
        this.kind = "stripePluginPending";
      }
    };
    function _2(e) {
      if ("stripe" in e && "resubmitKey" in e && typeof e.resubmitKey == "string") {
        let { stripe: r } = e;
        return typeof r == "object" && r != null && "paymentIntentClientSecret" in r && typeof r.paymentIntentClientSecret == "string";
      }
      return false;
    }
    function A(e) {
      return e.kind === "error";
    }
    var p2 = class {
      constructor(...r) {
        this.kind = "error";
        this.formErrors = [];
        this.fieldErrors = /* @__PURE__ */ new Map();
        var t;
        for (let o of r) {
          if (!o.field) {
            this.formErrors.push({ code: o.code && z2(o.code) ? o.code : "UNSPECIFIED", message: o.message });
            continue;
          }
          let s = (t = this.fieldErrors.get(o.field)) != null ? t : [];
          s.push({ code: o.code && Q(o.code) ? o.code : "UNSPECIFIED", message: o.message }), this.fieldErrors.set(o.field, s);
        }
      }
      getFormErrors() {
        return [...this.formErrors];
      }
      getFieldErrors(r) {
        var t;
        return (t = this.fieldErrors.get(r)) != null ? t : [];
      }
      getAllFieldErrors() {
        return Array.from(this.fieldErrors);
      }
    };
    function z2(e) {
      return e in B2;
    }
    var B2 = { BLOCKED: "BLOCKED", EMPTY: "EMPTY", FILES_TOO_BIG: "FILES_TOO_BIG", FORM_NOT_FOUND: "FORM_NOT_FOUND", INACTIVE: "INACTIVE", NO_FILE_UPLOADS: "NO_FILE_UPLOADS", PROJECT_NOT_FOUND: "PROJECT_NOT_FOUND", TOO_MANY_FILES: "TOO_MANY_FILES" };
    function Q(e) {
      return e in Z;
    }
    var Z = { REQUIRED_FIELD_EMPTY: "REQUIRED_FIELD_EMPTY", REQUIRED_FIELD_MISSING: "REQUIRED_FIELD_MISSING", STRIPE_CLIENT_ERROR: "STRIPE_CLIENT_ERROR", STRIPE_SCA_ERROR: "STRIPE_SCA_ERROR", TYPE_EMAIL: "TYPE_EMAIL", TYPE_NUMERIC: "TYPE_NUMERIC", TYPE_TEXT: "TYPE_TEXT" };
    function P2(e) {
      return "errors" in e && Array.isArray(e.errors) && e.errors.every((r) => typeof r.message == "string") || "error" in e && typeof e.error == "string";
    }
    var D2 = "4.0.0";
    var v2 = (e) => I2(JSON.stringify(e));
    var N2 = (e) => {
      let r = `@formspree/core@${D2}`;
      return e ? `${e} ${r}` : r;
    };
    function E2(e, r, t) {
      e instanceof FormData ? e.append(r, t) : e[r] = t;
    }
    function M2(e) {
      return e !== null && typeof e == "object";
    }
    var C = class {
      constructor(r = {}) {
        this.project = r.project, this.stripe = r.stripe, typeof window != "undefined" && (this.session = new y2());
      }
      submitForm(s, i) {
        return h2(this, arguments, function* (r, t, o = {}) {
          let c = o.endpoint || "https://formspree.io", l = this.project ? `${c}/p/${this.project}/f/${r}` : `${c}/f/${r}`, a = { Accept: "application/json", "Formspree-Client": N2(o.clientName) };
          this.session && (a["Formspree-Session-Data"] = v2(this.session.data())), t instanceof FormData || (a["Content-Type"] = "application/json");
          function m(f) {
            return h2(this, null, function* () {
              try {
                let n2 = yield (yield fetch(l, { method: "POST", mode: "cors", body: f instanceof FormData ? f : JSON.stringify(f), headers: a })).json();
                if (M2(n2)) {
                  if (P2(n2)) return Array.isArray(n2.errors) ? new p2(...n2.errors) : new p2({ message: n2.error });
                  if (_2(n2)) return new b(n2.stripe.paymentIntentClientSecret, n2.resubmitKey);
                  if (w2(n2)) return new S2({ next: n2.next });
                }
                return new p2({ message: "Unexpected response format" });
              } catch (d) {
                let n2 = d instanceof Error ? d.message : `Unknown error while posting to Formspree: ${JSON.stringify(d)}`;
                return new p2({ message: n2 });
              }
            });
          }
          if (this.stripe && o.createPaymentMethod) {
            let f = yield o.createPaymentMethod();
            if (f.error) return new p2({ code: "STRIPE_CLIENT_ERROR", field: "paymentMethod", message: "Error creating payment method" });
            E2(t, "paymentMethod", f.paymentMethod.id);
            let d = yield m(t);
            if (d.kind === "error") return d;
            if (d.kind === "stripePluginPending") {
              let n2 = yield this.stripe.handleCardAction(d.paymentIntentClientSecret);
              if (n2.error) return new p2({ code: "STRIPE_CLIENT_ERROR", field: "paymentMethod", message: "Stripe SCA error" });
              t instanceof FormData ? t.delete("paymentMethod") : delete t.paymentMethod, E2(t, "paymentIntent", n2.paymentIntent.id), E2(t, "resubmitKey", d.resubmitKey);
              let x = yield m(t);
              return k2(x), x;
            }
            return d;
          }
          let T = yield m(t);
          return k2(T), T;
        });
      }
    };
    function k2(e) {
      let { kind: r } = e;
      if (r !== "success" && r !== "error") throw new Error(`Unexpected submission result (kind: ${r})`);
    }
    var R = (e) => new C(e);
    var U = () => (F2 || (F2 = R()), F2);
    var F2;
  }
});

// node_modules/@formspree/ajax/dist/index.mjs
var import_core = __toESM(require_dist(), 1);
var h = (e, r, t) => new Promise((o, d) => {
  var s = (a) => {
    try {
      m(t.next(a));
    } catch (u) {
      d(u);
    }
  }, l = (a) => {
    try {
      m(t.throw(a));
    } catch (u) {
      d(u);
    }
  }, m = (a) => a.done ? o(a.value) : Promise.resolve(a.value).then(s, l);
  m((t = t.apply(e, r)).next());
});
var n = { ERROR: "data-fs-error", SUCCESS: "data-fs-success", FIELD: "data-fs-field", SUBMIT_BTN: "data-fs-submit-btn", ACTIVE: "data-fs-active" };
var V = "https://formspree.io";
var p = (e, r) => {
  console.log(`[formspree-ajax] ${e}`, r != null ? r : "");
};
var D = (e) => {
  if (typeof e == "string") {
    let r = document.querySelector(e);
    if (!r) throw new Error(`Element "${e}" not found`);
    if (!(r instanceof HTMLFormElement)) throw new Error(`Element "${e}" is not a form element`);
    return r;
  }
  return e;
};
var y = "data-fs-server-content";
var S = (e, r) => {
  var t;
  r && ((t = e.textContent) == null ? void 0 : t.trim()) === "" && (e.textContent = r, e.setAttribute(y, "")), e.setAttribute(n.ACTIVE, "");
};
var E = (e) => {
  e.removeAttribute(n.ACTIVE), e.hasAttribute(y) && (e.textContent = "", e.removeAttribute(y));
};
var L = (e, r) => {
  var d;
  let { form: t } = e, o = document.createElement("div");
  o.textContent = "Thank you!", (d = t.parentNode) == null || d.replaceChild(o, t);
};
var v = (e) => {
  e.form.removeAttribute("aria-busy"), e.form.querySelectorAll(`[type='submit']:disabled, [${n.SUBMIT_BTN}]:disabled`).forEach((t) => {
    let o = t.dataset.fsOriginalText;
    o && (t.textContent = o, delete t.dataset.fsOriginalText), t.disabled = false;
  });
};
var H = (e) => {
  e.form.setAttribute("aria-busy", "true"), e.form.querySelectorAll(`[type='submit']:enabled, [${n.SUBMIT_BTN}]:enabled`).forEach((t) => {
    var o;
    t.dataset.fsOriginalText = (o = t.textContent) != null ? o : "", t.disabled = true;
  });
};
var M = (e, r) => {
  let { form: t } = e;
  t.querySelectorAll(`[${n.ERROR}]`).forEach((s) => {
    let l = s.dataset.fsError;
    if (!l) return;
    if (!r) {
      E(s);
      return;
    }
    let m = r.getFieldErrors(l);
    if (m.length === 0) {
      E(s);
      return;
    }
    S(s, m[0].message);
  }), t.querySelectorAll(`[${n.FIELD}]`).forEach((s) => {
    let l = s.getAttribute("name");
    if (!l) {
      s.removeAttribute("aria-invalid");
      return;
    }
    if (!r) {
      s.removeAttribute("aria-invalid");
      return;
    }
    if (r.getFieldErrors(l).length === 0) {
      s.removeAttribute("aria-invalid");
      return;
    }
    s.setAttribute("aria-invalid", "true");
  });
};
var $ = (e, r) => {
  var t, o, d;
  return (d = (o = e.querySelector(r)) != null ? o : (t = e.parentElement) == null ? void 0 : t.querySelector(r)) != null ? d : null;
};
var F = (e) => $(e, `[${n.SUCCESS}]`);
var w = (e) => $(e, `[${n.ERROR}=""]`);
var j = (e, r) => {
  let t = F(e.form), o = w(e.form);
  if (r === null) {
    t && E(t);
    return;
  }
  o && E(o), t && S(t, r);
};
var N = (e, r) => {
  let t = F(e.form), o = w(e.form);
  if (r === null) {
    o && E(o);
    return;
  }
  t && E(t), o && S(o, r);
};
var O = (e) => e.getFormErrors().map((t) => t.message).join(", ") || "There was an error submitting the form.";
var I = false;
var k = () => {
  if (I || typeof document == "undefined") return;
  let e = document.createElement("style");
  e.setAttribute("data-formspree-styles", ""), e.textContent = `
    [${n.ERROR}],
    [${n.SUCCESS}] {
      display: none;
    }

    [${n.ERROR}][${n.ACTIVE}] {
      display: block;
      color: #dc3545;
    }

    [${n.ERROR}=""][${n.ACTIVE}] {
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
      background: #f8d7da;
      border: 1px solid #f5c6cb;
    }

    [${n.ERROR}]:not([${n.ERROR}=""])[${n.ACTIVE}] {
      font-size: 12px;
      margin-top: 4px;
    }

    [${n.SUCCESS}][${n.ACTIVE}] {
      display: block;
      padding: 12px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-size: 14px;
      background: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
    }

    [${n.FIELD}][aria-invalid="true"] {
      border-color: #dc3545;
    }
  `, document.head.appendChild(e), I = true;
};
var z = (e, r) => h(void 0, null, function* () {
  if (r.submitting) return;
  r.submitting = true;
  let { formKey: t, endpoint: o, client: d, config: s } = e, { debug: l, data: m, onSubmit: a, onSuccess: u, onError: f, onFailure: c, enable: b = v, disable: U = H, renderFieldErrors: R = M, renderSuccess: A = j, renderFormError: T = N } = s;
  R(e, null), A(e, null), T(e, null), U(e), a == null || a(e);
  let C = new FormData(e.form);
  if (m) for (let [i, g] of Object.entries(m)) {
    let x;
    typeof g == "function" ? x = yield g() : x = g, x !== void 0 && (0, import_core.appendExtraData)(C, i, x);
  }
  l && p("Submitting form", { formKey: t, formData: C });
  try {
    let i = yield d.submitForm(t, C, { clientName: "@formspree/ajax", endpoint: o });
    (0, import_core.isSubmissionError)(i) ? (l && p("Submission error", i), i.getAllFieldErrors().length > 0 && R(e, i), i.getFormErrors().length > 0 && T(e, O(i)), f == null || f(e, i)) : (l && p("Submission success", i), A(e, "Thank you!"), u ? u(e, i) : F(e.form) ? e.form.reset() : L(e, i));
  } catch (i) {
    l && console.error("[formspree-ajax] Unexpected error", i), T(e, "An unexpected error occurred. Please try again."), c == null || c(e, i);
  } finally {
    r.submitting = false, b(e);
  }
});
var K = (e) => {
  var u, f, c;
  if (e.useDefaultStyles !== false && k(), !e.formElement) throw new Error("You must provide a `formElement` in the config");
  if (!e.formId) throw new Error("You must provide a `formId` in the config");
  let r = D(e.formElement), t = e.formId, o = (u = e.origin) != null ? u : V, d = e.projectId ? (0, import_core.createClient)({ project: e.projectId }) : (0, import_core.getDefaultClient)(), s = { form: r, formKey: t, endpoint: o, client: d, config: e }, l = (f = e.enable) != null ? f : v;
  e.debug && p("Initializing form", s);
  let m = { submitting: false }, a = (b) => {
    b.preventDefault(), z(s, m);
  };
  return r.addEventListener("submit", a), l(s), (c = e.onInit) == null || c.call(e, s), { destroy: () => {
    r.removeEventListener("submit", a);
  } };
};
export {
  n as DataAttributes,
  K as initForm
};
//# sourceMappingURL=@formspree_ajax.js.map
